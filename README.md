# statistical-tests
A collection of statistical tests used in research

# usage
1. Install Python 2.7
2. Run the following with pip
`python -m pip install --user numpy scipy matplotlib ipython jupyter pandas sympy nose`
